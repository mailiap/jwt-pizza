# Incident: 2025-04-08 1:21:31 MT

## Summary

The JWT Headquarters Pizza Factory failed to create pizzas for a period of 5 minutes. This impacted diners’ ability to place orders, causing a temporary service problem. The error was caused by a failure in the factory service. The issue was diagnosed and reported quickly, and functionality was fully restored after the chaos was resolved.

## Detection

This incident was detected when no alert was triggered, and no team or person was paged. Instead, the issue was discovered manually when I checked my logs on Grafana and noticed something was wrong. Because there was no automated alerting, the response was delayed until I manually investigated the logs.

To improve this, alerting will be set up by the team so that similar failures are detected automatically, allowing for faster response times and reduce relying on manual checks.

## Impact

For about 20 minutes between 13:31 MT and 13:51 MT on 04/08/25, the JWT Headquarters Pizza Factory was unable to create pizzas due to a failure in the factory service. 

This incident affected all customers attempting to order pizzas during that time.

No support tickets or social media posts were submitted, as the issue was detected through manual log checks rather than user reports.

## Timeline

All times are Mountain Time (MT).

- _13:31_ - MaiLia checks Grafana logs and notices repeated errors;
- _13:31_ - No alert was received.
- _13:33_ - MaiLia attempts to make a pizza and receives an error.
- _13:45_ - MaiLia identifies grader-resolution URL in the error message.
- _13:50_ - MaiLia sends fix request to the grader using the resolution URL.
- _13:51_ - Grader instructs Factory to stop chaos.
- _13:51_ - Factory returns to normal operation and pizza creation resumes successfully.

## Response

MaiLia detected the incident by checking Grafana logs at 13:31 MT. No alert was triggered, so no on-call team was paged. MaiLia manually investigated, found the error source, and requested a fix at 1:38 PM MT. The grader responded by stopping the chaos injection, and normal service was restored by 13:50 MT. The lack of an automated alert caused a delay in identifying and responding to the issue.

## Root cause

A bug in JWT Headquarters pizza ordering system was determined to be the cause of the failure.

## Resolution

No alert was triggered, so no on-call team was paged. The issue was manually investigated, found the error source, and requested a fix. Using the issue reporting URL provided by JWT Headquarters the failure immediately resolved.

## Prevention

Automated alerting for failures will be implemented to ensure quicker detection and response. 

## Action items

1. Implement automated alerting for JWT token validation failures to ensure immediate detection of similar issues.
2. Develop and deploy an automation for failures to minimize manual action.
